<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aiwechat.auth.repository.UserRepository">
    <resultMap id="UserResultMap" type="com.aiwechat.auth.model.entity.User">
        <id property="id" column="id"/>
        <result property="openId" column="open_id"/>
        <result property="nickname" column="nickname"/>
        <result property="avatarUrl" column="avatar_url"/>
        <result property="status" column="status"/>
        <result property="createdAt" column="created_at"/>
        <result property="lastLoginAt" column="last_login_at"/>
    </resultMap>
    <sql id="Base_Column_List">
        id, open_id, nickname, avatar_url, status, created_at, last_login_at
    </sql>
    <select id="findByOpenId" resultMap="UserResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM users
        WHERE open_id = #{openId}
    </select>
    <select id="findById" resultMap="UserResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM users
        WHERE id = #{id}
    </select>
    <insert id="insert" parameterType="com.aiwechat.auth.model.entity.User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO users (open_id, nickname, avatar_url, status, created_at, last_login_at)
        VALUES (#{openId}, #{nickname}, #{avatarUrl}, #{status}, #{createdAt}, #{lastLoginAt})
    </insert>
    <update id="update" parameterType="com.aiwechat.auth.model.entity.User">
        UPDATE users
        SET nickname = #{nickname},
            avatar_url = #{avatarUrl}
        WHERE open_id = #{openId}
    </update>
    <update id="updateLastLogin">
        UPDATE users
        SET last_login_at = NOW()
        WHERE open_id = #{openId}
    </update>
</mapper>
