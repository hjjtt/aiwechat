<view class="container">
  <!-- 头部 -->
  <view class="header">
    <view class="header-title">我的收藏</view>
    <view class="header-sub">共 {{favorites.length}} 个收藏</view>
  </view>

  <!-- 收藏列表 -->
  <scroll-view class="favorites-scroll" scroll-y="true" wx:if="{{favorites.length > 0}}">
    <block wx:for="{{favorites}}" wx:key="id">
      <view class="favorite-card">
        <view class="card-emoji">
          {{item.menuCategory === '主食' ? '🍜' : item.menuCategory === '热菜' ? '🍛' : item.menuCategory === '素菜' ? '🥦' : item.menuCategory === '汤类' ? '🍲' : item.menuCategory === '饮品' ? '🥤' : '🍽️'}}
        </view>
        <view class="card-info">
          <view class="card-name">{{item.menuName}}</view>
          <view class="card-desc">{{item.menuDescription || '暂无描述'}}</view>
          <view class="card-footer">
            <view class="price-wrap">
              <text class="price-symbol">¥</text>
              <text class="price-num">{{item.priceFormatted}}</text>
            </view>

            <!-- 数量控制 -->
            <view class="quantity-control">
              <view
                class="qty-btn decrease {{item.quantity > 0 ? 'active' : 'disabled'}}"
                bindtap="decreaseQuantity"
                data-item="{{item}}"
              >
                <text>-</text>
              </view>
              <text class="qty-value {{item.quantity > 0 ? 'show' : 'hide'}}">{{item.quantity}}</text>
              <view
                class="qty-btn increase active"
                bindtap="increaseQuantity"
                data-item="{{item}}"
              >
                <text>+</text>
              </view>
            </view>
          </view>
        </view>
        <view class="card-actions">
          <view class="action-btn remove" bindtap="toggleFavorite" data-item="{{item}}">取消收藏</view>
        </view>
      </view>
    </block>

    <!-- 底部占位 -->
    <view class="bottom-placeholder"></view>
  </scroll-view>

  <!-- 空状态 -->
  <view class="empty-state" wx:else>
    <view class="empty-icon">❤️</view>
    <view class="empty-text">暂无收藏</view>
    <view class="empty-tip">收藏您喜欢的菜品，随时购买</view>
    <view class="empty-btn" bindtap="goHome">去点餐</view>
  </view>

  <!-- 购物车底部栏 -->
  <view class="cart-bar" wx:if="{{cartCount > 0}}">
    <view class="cart-main">
      <view class="cart-icon-wrap" bindtap="showCartPopup">
        <text class="cart-icon">🛒</text>
        <view class="cart-badge">{{cartCount}}</view>
      </view>
      <view class="cart-total">
        <text class="total-label">合计：</text>
        <text class="total-price">¥{{cartTotal}}</text>
      </view>
    </view>
    <view class="checkout-btn" bindtap="checkout">
      <text>去结算</text>
    </view>
  </view>

  <!-- 购物车弹窗 -->
  <view class="cart-popup-mask" wx:if="{{showCartPopup}}" bindtap="closeCartPopup"></view>
  <view class="cart-popup {{showCartPopup ? 'show' : ''}}">
    <view class="cart-header">
      <text class="cart-title">购物车</text>
      <view class="clear-btn" bindtap="clearCart">清空</view>
    </view>
    <scroll-view class="cart-items" scroll-y="true">
      <block wx:for="{{cart}}" wx:key="*this">
        <view class="cart-item">
          <view class="cart-item-info">
            <view class="cart-item-name">{{item.item.name}}</view>
            <view class="cart-item-price">¥{{item.item.priceFormatted}}</view>
          </view>
          <view class="cart-item-qty">
            <view
              class="qty-btn decrease active"
              bindtap="decreaseQuantity"
              data-item="{{item.item}}"
            >
              <text>-</text>
            </view>
            <text class="qty-number">{{item.quantity}}</text>
            <view
              class="qty-btn increase active"
              bindtap="increaseQuantity"
              data-item="{{item.item}}"
            >
              <text>+</text>
            </view>
          </view>
        </view>
      </block>
    </scroll-view>
    <view class="cart-footer">
      <view class="cart-total-wrap">
        <text class="cart-total-label">合计：</text>
        <text class="cart-total-price">¥{{cartTotal}}</text>
      </view>
      <view class="checkout-btn large" bindtap="checkout">去结算</view>
    </view>
  </view>
</view>
